/**
 * ==========================================================
 * Quiz Screen
 * Renders the quiz interface.
 * ==========================================================
 */

"use strict";

/**
 * Render quiz screen.
 *
 * @returns {string}
 */
export function renderQuizPage() {
  return `

        <main class="quiz">

            <section class="card">

                <h2>Quiz Started 🚀</h2>

                <p>This is where the quiz will appear.</p>

            </section>

        </main>

    `;
}
